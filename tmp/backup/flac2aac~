#!/bin/bash
#
# Flac to aac converter for my music.

CURDIR="$(pwd)"

find "$CURDIR" -type f | grep -E .flac$ | while read file
do
  ffmpeg -i "$file" -acodec alac -map_metadata 0:0,s0 "$(basename "$file").flac"
# if [[ "$!" -eq "0" ]]
# then
#   rm "$fn.flac"
# fi
done

